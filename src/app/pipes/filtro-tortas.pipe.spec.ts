import { FiltroTortasPipe } from './filtro-tortas.pipe';

describe('FiltroTortasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTortasPipe();
    expect(pipe).toBeTruthy();
  });
});
